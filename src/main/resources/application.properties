# Server
server.port=${PORT:8080}

# Connection Pool (important for production)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.vhqdqaeqqucobdlzxhtp
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Disable open-in-view (best practice for production)
spring.jpa.open-in-view=false

# Logging
logging.level.root=INFO
logging.level.com.example.cftcbrandtech=INFO
logging.level.org.hibernate.SQL=WARN

# CORS - Set your frontend domain(s) here
# For multiple domains: https://app.example.com,https://admin.example.com
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}